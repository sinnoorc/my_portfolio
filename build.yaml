targets:
  $default:
    auto_apply_builders: true
    builders:
      freezed:
        enabled: true
        generate_for:
          include:
            - lib/**.dart
      json_serializable:
        enabled: true
        generate_for:
          include:
            - lib/**.dart
      riverpod_generator:
        enabled: true
        generate_for:
          include:
            - lib/**.dart